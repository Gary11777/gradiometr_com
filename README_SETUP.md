# Garand 101 - Градиометр-магнитометр Website

Современный, отзывчивый веб-сайт на Laravel для продвижения градиометра-магнитометра Garand 101.

## Особенности

### Технологический стек
- **Backend**: Laravel 11
- **Frontend**: Tailwind CSS v4 (как модуль)
- **JavaScript**: Alpine.js v3 (как модуль)
- **Язык**: Русский

### Функциональность

#### Страницы сайта
1. **Главная** (`/`) - Героический раздел с ключевыми преимуществами
2. **Преимущества** (`/advantages`) - Подробное описание преимуществ устройства
3. **Характеристики** (`/specifications`) - Полные технические характеристики
4. **Сравнение** (`/comparison`) - Сравнительная таблица с конкурентами
5. **Фото** (`/photos`) - Фотогалерея устройства
6. **Купить** (`/buy`) - Форма заказа с защитой
7. **О нас** (`/about`) - Информация о компании и контактная форма

#### Безопасность контактной формы
- ✅ **Cloudflare Turnstile** - Невидимая защита от ботов
- ✅ **Honeypot поле** - Ловушка для простых ботов
- ✅ **Rate Limiting** - Защита от злоупотреблений (5 попыток/минуту)
- ✅ **CSRF Protection** - Защита от межсайтовых атак
- ✅ **Server-side Validation** - Валидация и санитизация данных

#### Защита от спама
- 📧 **Email обфускация** - JavaScript защита email адресов
- 📱 **Phone обфускация** - Защита телефонных номеров от спам-ботов

#### Дизайн
- 🎨 Современный минималистичный дизайн
- 🌈 Цветовая палитра вдохновлена adaline.ai и tars.pro
- ✨ Тонкие анимации и спецэффекты
- 📱 Полностью адаптивный дизайн (mobile-first)
- 🎯 Четкая визуальная иерархия
- ♿ Доступность и контрастность

## Установка и настройка

### Требования
- PHP 8.2+
- Composer
- Node.js 18+ и npm
- Laravel Herd (рекомендуется) или другой локальный сервер

### Шаги установки

1. **Клонируйте репозиторий** (если применимо)
```bash
git clone <repository-url>
cd gradiometr_com
```

2. **Установите PHP зависимости**
```bash
composer install
```

3. **Установите JavaScript зависимости**
```bash
npm install
```

4. **Настройте окружение**
```bash
cp .env.example .env
php artisan key:generate
```

5. **Обновите .env файл**
```env
APP_NAME=Gradiometr
APP_LOCALE=ru
APP_FALLBACK_LOCALE=ru

# Настройки почты для sendmail
MAIL_MAILER=sendmail
MAIL_SENDMAIL_PATH="/usr/sbin/sendmail -t -i"
MAIL_FROM_ADDRESS="info@gradiometr.com"
MAIL_FROM_NAME="Gradiometr"

# Cloudflare Turnstile (получите ключи на https://dash.cloudflare.com/)
TURNSTILE_SITE_KEY=your_site_key_here
TURNSTILE_SECRET_KEY=your_secret_key_here
VITE_TURNSTILE_SITE_KEY="${TURNSTILE_SITE_KEY}"
```

6. **Создайте базу данных**
```bash
touch database/database.sqlite
php artisan migrate
```

7. **Соберите фронтенд ресурсы**

Для разработки:
```bash
npm run dev
```

Для продакшена:
```bash
npm run build
```

8. **Запустите сервер**

С Laravel Herd:
```bash
herd open
```

Или с встроенным сервером:
```bash
php artisan serve
```

## Настройка Cloudflare Turnstile

1. Зарегистрируйтесь на [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в раздел Turnstile
3. Создайте новый сайт
4. Выберите тип: "Invisible" для лучшего UX
5. Скопируйте Site Key и Secret Key
6. Добавьте их в `.env` файл

## Структура проекта

```
gradiometr_com/
├── app/
│   └── Http/
│       └── Controllers/
│           └── ContactController.php    # Контроллер контактной формы
├── config/
│   └── services.php                     # Конфигурация Turnstile
├── resources/
│   ├── css/
│   │   └── app.css                      # Tailwind CSS с кастомными стилями
│   ├── js/
│   │   └── app.js                       # Alpine.js конфигурация
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php            # Главный layout
│       ├── emails/
│       │   └── contact.blade.php        # Email шаблон
│       ├── home.blade.php               # Главная страница
│       ├── advantages.blade.php         # Преимущества
│       ├── specifications.blade.php     # Характеристики
│       ├── comparison.blade.php         # Сравнение
│       ├── photos.blade.php             # Фото
│       ├── buy.blade.php                # Купить
│       └── about.blade.php              # О нас
└── routes/
    └── web.php                          # Маршруты приложения
```

## Кастомизация

### Изменение контактной информации

**Телефон**: Обновите в `resources/views/layouts/app.blade.php` и `resources/views/about.blade.php`:
```javascript
Alpine.data('obfuscatedPhone', () => ({
    parts: ['+375', '29 ', '636-35-97']  // Измените здесь
}))
```

**Email**: Обновите в `resources/js/app.js`:
```javascript
window.showEmail = function() {
    const user = "info";                  // Измените здесь
    const domain = "gradiometr.com";      // Измените здесь
    window.location.href = "mailto:" + user + "@" + domain;
};
```

### Изменение цветовой схемы

Отредактируйте `resources/css/app.css` для изменения градиентов и цветов:
```css
.gradient-text {
    @apply bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent;
}
```

### Добавление фотографий

Разместите изображения в `public/images/` и обновите соответствующие view файлы.

## Развертывание в продакшен

1. **Соберите ресурсы**
```bash
npm run build
```

2. **Оптимизируйте автозагрузку**
```bash
composer install --optimize-autoloader --no-dev
```

3. **Кэшируйте конфигурацию**
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

4. **Настройте окружение**
- Установите `APP_ENV=production`
- Установите `APP_DEBUG=false`
- Используйте надежный `APP_KEY`
- Настройте правильные настройки почты

5. **Настройте веб-сервер**
- Укажите document root на `/public`
- Настройте SSL сертификат
- Включите gzip сжатие
- Настройте кэширование статических ресурсов

## Тестирование контактной формы

1. Убедитесь, что Turnstile ключи настроены
2. Откройте страницу `/buy` или `/about`
3. Заполните форму
4. Проверьте логи Laravel для отправленных писем (если используется `MAIL_MAILER=log`)

## Поддержка и обслуживание

### Логи
Проверяйте логи в `storage/logs/laravel.log` для отладки:
```bash
tail -f storage/logs/laravel.log
```

### Очистка кэша
```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Обновление зависимостей
```bash
composer update
npm update
```

## Безопасность

- ✅ CSRF защита включена по умолчанию
- ✅ Rate limiting на контактной форме
- ✅ Валидация всех входных данных
- ✅ Email и телефон защищены от спам-ботов
- ✅ Cloudflare Turnstile для защиты от ботов
- ✅ Honeypot поле для ловли простых ботов

## Производительность

- ⚡ Tailwind CSS v4 с оптимизацией
- ⚡ Alpine.js для легкой интерактивности
- ⚡ Минимальное количество JavaScript
- ⚡ Оптимизированные изображения (рекомендуется WebP)
- ⚡ Кэширование на уровне браузера

## Лицензия

Все права защищены © 2026 Garand 101

## Контакты

- **Email**: info@gradiometr.com
- **Телефон**: +375 29 636-35-97
- **Время работы**: Пн-Пт: 9:00 - 18:00

---

**Разработано с ❤️ для Garand 101**
